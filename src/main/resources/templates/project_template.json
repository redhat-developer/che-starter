[{
  "name": "${project.name}",
  "projectType": "maven",
  "description": "A hello world Java application.",
  "path": "/${project.name}",
  "source": {
    "parameters": {
      "branch": "${project.git.branch}",
      "keepVcs": "true"
    },
    "type": "git",
    "location": "${project.git.repo}"
  },
  "attributes": {},
  "links": [ { } ],
  "mixins": [
    "git"
  ]
}]